# 🃏 Green Agent Benchmark - 使用指南

这是一个用于评估 LLM 和规则型智能体在 **无限注德州扑克 (No-Limit Texas Hold'em)** 中表现的 Benchmark 系统。支持 Heads-Up (单挑) 和 6-Max (六人桌) 两种模式。

---

## 📋 目录

1. [快速开始](#-快速开始)
2. [环境配置](#-环境配置)
3. [运行 Benchmark](#-运行-benchmark)
4. [配置文件详解](#-配置文件详解)
5. [支持的 Agent](#-支持的-agent)
6. [自定义 Agent](#-自定义-agent)
7. [输出与指标](#-输出与指标)
8. [排行榜系统](#-排行榜系统)
9. [常用命令速查](#-常用命令速查)

---

## 🚀 快速开始

### 1. 安装依赖

```bash
# 创建虚拟环境
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
```

### 2. 配置 API Key

在项目根目录创建 `.env` 文件，配置各模型的 API Key：

```bash
# OpenAI (GPT-5)
OPENAI_API_KEY=sk-xxx

# DeepSeek
DEEPSEEK_API_KEY=sk-xxx

# Gemini
GEMINI_API_KEY=xxx
GEMINI_API_BASE=https://your-proxy/v1  # 可选，使用代理时配置

# Kimi (月之暗面)
KIMI_API_KEY=xxx

# Qwen (通义千问)
QWEN_API_KEY=xxx

# Doubao (豆包)
DOUBAO_API_KEY=xxx

# GLM (智谱)
GLM_API_KEY=xxx

# Cohere
COHERE_API_KEY=xxx
```

### 3. 运行一个简单测试

```bash
python -m green_agent_benchmark.cli \
  --config configs/demo_hu_10hands.yaml \
  --agent baseline:random-hu \
  --output artifacts/quick_test
```

---

## ⚙️ 环境配置

### 环境变量

每个 LLM Agent 需要配置对应的环境变量：

| Provider | API Key 变量 | Model 变量 (可选) | Base URL 变量 (可选) |
|----------|-------------|------------------|---------------------|
| OpenAI   | `OPENAI_API_KEY` | `OPENAI_MODEL` | `OPENAI_API_BASE` |
| DeepSeek | `DEEPSEEK_API_KEY` | `DEEPSEEK_MODEL` | `DEEPSEEK_API_BASE` |
| Gemini   | `GEMINI_API_KEY` | `GEMINI_MODEL` | `GEMINI_API_BASE` |
| Kimi     | `KIMI_API_KEY` | `KIMI_MODEL` | `KIMI_API_BASE` |
| Qwen     | `QWEN_API_KEY` | `QWEN_MODEL` | `QWEN_API_BASE` |
| Doubao   | `DOUBAO_API_KEY` | `DOUBAO_MODEL` | `DOUBAO_API_BASE` |
| GLM      | `GLM_API_KEY` | `GLM_MODEL` | `GLM_API_BASE` |
| Cohere   | `COHERE_API_KEY` | `COHERE_MODEL` | `COHERE_API_BASE` |

---

## 🎮 运行 Benchmark

### 基本命令格式

```bash
python -m green_agent_benchmark.cli \
  --config <配置文件路径> \
  --agent <agent规格> \
  --agent-name <显示名称> \
  --output <输出目录>
```

### 参数说明

| 参数 | 必需 | 说明 |
|------|------|------|
| `--config` | ✅ | YAML 配置文件路径 |
| `--agent` | ❌ | Agent 规格，格式为 `baseline:<名称>` 或 `模块:类名` |
| `--agent-name` | ❌ | 覆盖 Agent 的显示名称 |
| `--output` | ❌ | 输出目录，默认为 `artifacts/latest_run` |

### 运行示例

#### 示例 1: 使用内置 Baseline Agent

```bash
# Random Agent (随机策略)
python -m green_agent_benchmark.cli \
  --config configs/dev_hu.yaml \
  --agent baseline:random-hu \
  --output artifacts/random_test

# TAG Agent (紧凶策略)
python -m green_agent_benchmark.cli \
  --config configs/dev_hu.yaml \
  --agent baseline:tag-hu \
  --output artifacts/tag_test
```

#### 示例 2: LLM Agent 对决

```bash
# GPT-5 vs DeepSeek
export OPENAI_API_KEY=sk-xxx
export DEEPSEEK_API_KEY=sk-xxx

python -m green_agent_benchmark.cli \
  --config configs/demo_hu_deepseek_vs_gpt5.yaml \
  --agent green_agent_benchmark.agents.gpt5_agent:GPT5Agent \
  --agent-name GPT5 \
  --output artifacts/gpt5_vs_deepseek
```

#### 示例 3: 6人桌 LLM 大乱斗

```bash
# 配置所有需要的 API Key
python -m green_agent_benchmark.cli \
  --config configs/sixmax_llm_showdown.yaml \
  --output artifacts/sixmax_llm
```

当配置文件中定义了完整的 `lineup` 时，不需要指定 `--agent` 参数。

---

## 📝 配置文件详解

配置文件使用 YAML 格式，位于 `configs/` 目录下。

### Heads-Up (单挑) 配置

```yaml
mode: hu                    # 模式: hu (单挑)
blinds:
  sb: 50                    # 小盲注
  bb: 100                   # 大盲注
stacks_bb: 100              # 起始筹码 (以大盲为单位，即 100BB = 10000)
seeds: [101, 102]           # 随机种子列表，控制牌局可复现性
hands_per_seed: 50          # 每个种子打多少手
replicas: 2                 # 重复次数 (用于控制方差)
opponent_mix:               # 对手池配置 (概率分布)
  deepseek-hu: 0.5          # 50% 的对局使用 DeepSeek
  gpt5-hu: 0.5              # 50% 的对局使用 GPT-5
```

### 6-Max (六人桌) 配置

```yaml
mode: sixmax                # 模式: sixmax (六人桌)
blinds:
  sb: 50
  bb: 100
stacks_bb: 100
seeds: [501]
seat_replicas: 6            # 座位轮换次数
hands_per_replica: 50       # 每次轮换打多少手
lineup:                     # 完整的六人阵容
  - baseline:glm-6
  - baseline:gpt5-6
  - baseline:gemini-6
  - baseline:deepseek-6
  - baseline:kimi-6
  - baseline:doubao-6
population_mirroring: true  # 是否启用镜像对局
```

### lineup 参数覆盖

可以在 lineup 中使用 query string 覆盖 Agent 参数：

```yaml
lineup:
  - baseline:gpt5-6?model=gpt-5&name=GPT5-Pro
  - baseline:gpt5-6?model=gpt-5-mini&name=GPT5-Mini
  - baseline:gemini-6
  - baseline:deepseek-6
  - baseline:kimi-6
  - baseline:qwen-6
```

---

## 🤖 支持的 Agent

### 内置 Baseline Agent

| Agent 名称 | HU 版本 | 6-Max 版本 | 说明 |
|-----------|---------|-----------|------|
| Random    | `random-hu` | `random-6` | 随机策略 |
| TAG       | `tag-hu` | `tag-6` | 紧凶策略 (Tight-Aggressive) |
| CFR-Lite  | `cfrlite-hu` | `cfrlite-6` | 基于 CFR 的简化求解器策略 |

### LLM Agent

| Provider | HU 版本 | 6-Max 版本 | 环境变量前缀 |
|----------|---------|-----------|-------------|
| GPT-5 (OpenAI) | `gpt5-hu` | `gpt5-6` | `OPENAI_` |
| DeepSeek | `deepseek-hu` | `deepseek-6` | `DEEPSEEK_` |
| Gemini | `gemini-hu` | `gemini-6` | `GEMINI_` |
| Kimi | `kimi-hu` | `kimi-6` | `KIMI_` |
| Qwen | `qwen-hu` | `qwen-6` | `QWEN_` |
| Doubao | `doubao-hu` | `doubao-6` | `DOUBAO_` |
| GLM | `glm-hu` | `glm-6` | `GLM_` |
| Cohere | `cohere-hu` | `cohere-6` | `COHERE_` |

---

## 🔧 自定义 Agent

### Agent 接口

创建自定义 Agent 需要实现以下接口：

```python
from green_agent_benchmark.schemas import ActionRequest, ActionResponse


class MyAgent:
    name = "my-agent"  # Agent 显示名称

    def reset(self, seat_id: int, table_config: dict) -> None:
        """每手牌开始前调用 (可选)"""
        pass

    def act(self, request: ActionRequest) -> ActionResponse:
        """决策函数 - 必须实现"""
        # request 包含：
        # - hole_cards: 手牌
        # - board: 公共牌
        # - pot: 底池
        # - to_call: 需要跟注的金额
        # - min_raise_to: 最小加注金额
        # - legal_actions: 合法动作列表 ["fold", "check", "call", "raise_to"]
        # - stacks: 各玩家筹码
        # - action_history: 行动历史
        
        # 返回决策
        return ActionResponse(action="fold")
        # 或
        return ActionResponse(action="raise_to", amount=200)
```

### ActionRequest 字段

```python
@dataclass
class ActionRequest:
    seat_count: int          # 座位数
    table_id: str            # 牌桌 ID
    hand_id: str             # 手牌 ID
    seat_id: int             # 你的座位号
    button_seat: int         # 庄家座位
    blinds: Dict[str, int]   # {"sb": 50, "bb": 100}
    stacks: Dict[int, int]   # 各座位筹码
    pot: int                 # 底池
    to_call: int             # 需要跟注金额
    min_raise_to: int        # 最小加注金额
    hole_cards: Sequence[str]  # 手牌，如 ["Ah", "Kd"]
    board: Sequence[str]     # 公共牌，如 ["Qh", "Jc", "Ts"]
    action_history: Sequence[ActionHistoryEntry]  # 行动历史
    legal_actions: Sequence[str]  # 合法动作
    timebank_ms: int         # 剩余思考时间
    rng_tag: str             # RNG 标签 (用于可复现性)
```

### 使用自定义 Agent

```bash
# 保存为 mybot.py
python -m green_agent_benchmark.cli \
  --config configs/dev_hu.yaml \
  --agent mybot:MyAgent \
  --output artifacts/mybot_run
```

---

## 📊 输出与指标

每次运行后，输出目录包含：

```
artifacts/<run_name>/
├── logs/
│   └── <mode>/
│       └── *.ndjson        # 每手牌的详细事件日志
├── metrics/
│   ├── per_hand_metrics.ndjson  # 每手牌的统计
│   └── metrics.json        # 汇总统计
```

### metrics.json 示例

```json
{
  "GPT5": {
    "hands": 100,
    "bb_per_100": -35.0,
    "bb_per_100_ci": [-40.0, -30.0],
    "match_points": -1,
    "timeouts": {"count": 0, "per_hand": 0.0},
    "illegal_actions": {"count": 1, "per_hand": 0.01},
    "behavior": {
      "vpip": {"count": 70, "rate": 0.70},
      "pfr": {"count": 40, "rate": 0.40},
      "af": 1.75,
      "wt_sd": {"count": 30, "rate": 0.60},
      "decision_time_ms": {"mean": 850.4, "samples": 220}
    }
  }
}
```

### 指标说明

| 指标 | 说明 |
|------|------|
| `bb_per_100` | 每 100 手赢/亏的大盲注数，核心评估指标 |
| `bb_per_100_ci` | 95% 置信区间 |
| `match_points` | +1(显著盈利), 0(无显著差异), -1(显著亏损) |
| `vpip` | 主动入池率 (Voluntarily Put In Pot) |
| `pfr` | 翻牌前加注率 (Pre-Flop Raise) |
| `af` | 攻击性系数 (Aggression Factor) |
| `wt_sd` | 摊牌率 (Went To Showdown) |
| `decision_time_ms` | 平均决策时间 |

---

## 🏆 排行榜系统

项目包含一个实时排行榜系统，用于展示和比较各 Agent 的表现。

### 启动排行榜

```bash
# 一键启动 (推荐)
python leaderboard/launcher.py

# 仅启动 Web 服务器
python leaderboard/launcher.py --server-only

# 自定义端口
python leaderboard/launcher.py --port 8080
```

### 访问排行榜

启动后访问 `http://localhost:8000` 查看排行榜。

### 功能

- 🔄 **实时更新**: 自动监控 `artifacts/` 目录
- 📊 **综合评分**: 基于 BB/100、一致性、技术质量的 Elo 风格评分
- 🎨 **现代界面**: 支持表格/卡片视图切换
- 🔍 **搜索筛选**: 按名称搜索，按条件筛选

---

## 📌 常用命令速查

### 快速测试

```bash
# 10 手快速测试
python -m green_agent_benchmark.cli \
  --config configs/demo_hu_10hands.yaml \
  --agent baseline:random-hu \
  --output artifacts/quick_test
```

### LLM 对决

```bash
# DeepSeek vs GPT-5
python -m green_agent_benchmark.cli \
  --config configs/demo_hu_deepseek_vs_gpt5.yaml \
  --agent green_agent_benchmark.agents.gpt5_agent:GPT5Agent \
  --agent-name GPT5 \
  --output artifacts/gpt5_vs_deepseek
```

### 批量运行

```bash
# 使用 start.sh 运行预设的多个对局
./start.sh
```

### 查看现有配置

```bash
ls configs/
ls configs/deepseek/
ls configs/gemini/
```

---

## 🌐 AgentBeats 部署指南

将你的 Agent 部署到 [AgentBeats](https://agentbeats.org) 平台，与其他 Agent 进行在线对战。

### 快速部署（一键脚本）

```bash
./start_agentbeats_with_ngrok.sh
```

此脚本会自动：
1. 启动 ngrok 进行内网穿透
2. 获取公网 URL 并更新 Agent Card
3. 启动 Agent 服务

### 手动部署步骤

#### 1. 安装并配置 ngrok

```bash
# macOS
brew install ngrok

# Linux (下载二进制)
wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
tar -xvzf ngrok-v3-stable-linux-amd64.tgz
sudo mv ngrok /usr/local/bin/

# 配置 authtoken (去 ngrok.com 注册获取)
ngrok config add-authtoken <your-token>
```

#### 2. 启动 Agent 服务

```bash
# 终端 1: 启动服务
./start_agentbeats.sh green
```

#### 3. 启动内网穿透

```bash
# 终端 2: 穿透 Agent 端口
ngrok http 8001
# 记下 URL，如 https://abc123.ngrok-free.dev
```

#### 4. 更新 Agent Card

编辑 `agentbeats/cards/texas_green_agent_card.toml`，将 `url` 改为 ngrok URL：

```toml
url = "https://abc123.ngrok-free.dev/"
```

#### 5. 在 AgentBeats 注册

访问 AgentBeats 平台，填写：

| 字段 | 值 |
|------|-----|
| **Name** | 你的 Agent 名称 |
| **Deploy Type** | Remote |
| **Controller URL** | 你的 ngrok URL（如 `https://abc123.ngrok-free.dev`） |
| **Is Assessor (Green) Agent** | ✅ 勾选（如果是裁判 Agent） |

### Agent 类型说明

| 类型 | 角色 | 端口 | 启动命令 |
|------|------|------|----------|
| Green Agent | 裁判/评估器 | 8001 | `./start_agentbeats.sh green` |
| Red Agent | 进攻方参赛者 | 9011 | `./start_agentbeats.sh red` |
| Blue Agent | 防守方参赛者 | 9021 | `./start_agentbeats.sh blue` |

### 验证部署

启动服务后，访问以下 URL 验证：

```bash
# 本地测试 Agent Card 端点
curl http://localhost:8001/.well-known/agent.json

# 通过 ngrok 测试
curl https://your-ngrok-url.ngrok-free.dev/.well-known/agent.json
```

### 参考文档

- [AgentBeats 官方教程](https://github.com/agentbeats/tutorial)
- 本项目 AgentBeats 集成文档: `docs/AGENTBEATS.md`

---

## 🔍 故障排除

### 常见问题

1. **API Key 未配置**
   - 检查 `.env` 文件是否存在
   - 确认环境变量名称正确

2. **超时错误**
   - LLM 响应慢时可能超时
   - 检查网络连接和 API 配置

3. **非法动作**
   - LLM 返回了无效的动作格式
   - 系统会自动 fallback 到安全动作 (check/fold)

4. **ImportError**
   - 确保已安装所有依赖: `pip install -r requirements.txt`

---

## 📚 更多资源

- 主 README: `README.md`
- 架构文档: `docs/ARCHITECTURE.md`
- AgentBeats 集成: `docs/AGENTBEATS.md`
- 排行榜文档: `leaderboard/README.md`

---

**Happy Benchmarking! 🃏🎰**

